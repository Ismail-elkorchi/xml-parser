# Replay Diagnostics

Replay diagnostics expose deterministic parser-event contracts across runtimes.

Exported APIs:
- `createXmlReplayContract()`
- `verifyXmlReplayContract()`

Use cases:
- capture parser event traces,
- verify replay stability,
- compare runtime output hashes.
