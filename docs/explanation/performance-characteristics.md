# Performance Characteristics

The parser targets predictable runtime behavior:
- bounded parse depth and node counts,
- deterministic replay contracts,
- stable cross-runtime output.

Performance and reliability commands:

```bash
npm run test
npm run check:fast
```
