# Design Constraints

- Deterministic parsing and serialization over permissive XML behavior.
- Explicit limits to bound memory and CPU usage.
- Stable errors and contracts for automation.
- Non-goal: full XML Schema execution inside parse flow.
